import { BaseComponent } from '@components/utils';
import { buildClasses } from '@utils/buildClasses';

import { {{pascalCase name}}Props } from './{{pascalCase name}}.types';
import {{camelCase name}}Classes from './{{pascalCase name}}.classes';

const {{pascalCase name}} = ({ title, styling, overrides }: {{pascalCase name}}Props) => {
  const classes = buildClasses({{camelCase name}}Classes, overrides);
  return (
    <BaseComponent as="div" className={classes.root} styling={styling} stylingOptions=\{{ atomicType: '{{atomicType}}' }}>
      {title}
    </BaseComponent>
  );
};

export default {{pascalCase name}};
