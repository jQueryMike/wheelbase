import { render } from '@testing-library/react';
import { axe } from 'jest-axe';

import {{pascalCase name}} from "./{{pascalCase name}}";

describe('{{pascalCase name}} test suite', () => {
  it('should work', () => {
    expect(true).toBe(true)
  })

  it('should have no accessibility violations', async () => {
    {{#if asynchronous}}
    const ResolvedComponent = await {{pascalCase name}}({
      title: '{{pascalCase name}}',
      spacing: {
        marginBottom: '0',
        marginTop: '0',
        marginLeft: '0',
        marginRight: '0',
        paddingBottom: '0',
        paddingTop: '0',
        paddingLeft: '0',
        paddingRight: '0',
      }
    });
    const { container } = render(ResolvedComponent);
    {{^}}
    const { container } = render(<{{pascalCase name}} title="{{pascalCase name}}" spacing={ {
        marginBottom: '0',
        marginTop: '0',
        marginLeft: '0',
        marginRight: '0',
        paddingBottom: '0',
        paddingTop: '0',
        paddingLeft: '0',
        paddingRight: '0',
    } }/>);
    {{/if}}
    const results = await axe(container);
    expect(results).toHaveNoViolations();
  });
});