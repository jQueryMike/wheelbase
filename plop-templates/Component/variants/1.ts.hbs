import { ClassesBuilder, ClassesProperty, tw } from '@utils';

import { {{pascalCase name}}Classes } from '../{{pascalCase name}}.types';
import {{pascalCase name}}Variant from './{{pascalCase name}}Variant';

const location = '{{pascalCase name}}/variants/1';

let classes: {{pascalCase name}}Classes<ClassesProperty> = {};

classes = {
  root: tw``,
};

const {{camelCase name}}Variant: {{pascalCase name}}Variant = { classes: new ClassesBuilder({ location, classes }).classes };

export default {{camelCase name}}Variant;
